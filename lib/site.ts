// Public origin for canonical URLs, sitemap and OG metadata.
// Set SITE_URL in the deploy environment; the default is the production domain.
export const SITE_URL = process.env.SITE_URL ?? "https://eatimate.app";
